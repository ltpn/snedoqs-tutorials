name: Docker

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

on:
  workflow_dispatch:
  workflow_call:
  schedule:
    - cron: '43 21 * * *'
  push:
    branches: [ "docker" ]
    # Publish semver tags as releases.
    tags: [ 'v*.*.*' ]
  pull_request:
    branches: [ "docker" ]

env:
  # Use docker.io for Docker Hub if empty
  # Use ghcr.io for GitHub
  # Use quay.io for Quay
  REGISTRY: ghcr.io
  USERNAME: ${{ github.actor }}
  PASSWORD: ${{ secrets.GITHUB_TOKEN }}
  # USERNAME: ${{ secrets.QUAY_USERNAME }}
  # PASSWORD: ${{ secrets.QUAY_TOKEN }}
  # github.repository as <account>/<repo>
  IMAGE_NAME: ${{ github.repository }}


jobs:
  build:
    strategy:
      matrix:
        platform: [amd64, arm64]
        include:
          - platform: amd64
            runner: ubuntu-24.04
            docker_platform: linux/amd64
          - platform: arm64
            runner: ubuntu-24.04-arm
            docker_platform: linux/arm64

    name: Build Docker image for ${{ matrix.docker_platform }}

    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
      packages: write
      # This is used to complete the identity challenge
      # with sigstore/fulcio when running outside of PRs.
      id-token: write

    steps:
      - name: Checkout docker branch
        uses: actions/checkout@v7
        with:
          ref: docker
          fetch-depth: 0

      - name: Checkout buildfiles on PR against main
        if: github.base_ref == 'main' && github.event.pull_request.number != ''
        uses: actions/checkout@v7
        with:
          path: buildfiles
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: Checkout buildfiles on push to main
        if: github.ref == 'refs/heads/main' && github.base_ref == ''
        uses: actions/checkout@v7
        with:
          path: buildfiles
          # default ref: github.sha
          fetch-depth: 0

      - name: Checkout buildfiles on PR / push to docker
        if: github.ref == 'refs/heads/docker' || github.base_ref == 'docker'
        uses: actions/checkout@v7
        with:
          path: buildfiles
          ref: main
          fetch-depth: 0

      - name: Copy buildfiles into workspace
        run: |
          cp buildfiles/requirements.txt requirements.txt
          cp buildfiles/Project.toml Project.toml
          cp buildfiles/LocalPreferences.toml LocalPreferences.toml

      - name: Check disk space pre-cleanup
        run: df . -h
        
      - name: Cleanup runner
        run: | 
          sudo docker rmi $(docker image ls -aq) 2>&1 || true
          sudo rm -rf \
            /usr/share/dotnet /usr/local/lib/android /opt/ghc \
            /usr/local/share/powershell /usr/share/swift /usr/local/.ghcup \
            /usr/lib/jvm || true
          echo "Some directories deleted"
          sudo apt install aptitude -y 2>&1 || true
          sudo aptitude purge aria2 ansible azure-cli shellcheck rpm xorriso zsync \
            esl-erlang firefox gfortran-8 gfortran-9 google-chrome-stable \
            google-cloud-sdk imagemagick \
            libmagickcore-dev libmagickwand-dev libmagic-dev ant ant-optional kubectl \
            mercurial apt-transport-https mono-complete libmysqlclient \
            unixodbc-dev yarn chrpath libssl-dev libxft-dev \
            libfreetype6 libfreetype6-dev libfontconfig1 libfontconfig1-dev \
            snmp pollinate libpq-dev postgresql-client powershell ruby-full \
            sphinxsearch subversion mongodb-org azure-cli microsoft-edge-stable \
            -y -f 2>&1 || true
          sudo aptitude purge google-cloud-sdk -f -y 2>&1 || true
          sudo aptitude purge microsoft-edge-stable -f -y 2>&1 || true
          sudo apt purge microsoft-edge-stable -f -y 2>&1 || true
          sudo aptitude purge '~n ^mysql' -f -y 2>&1 || true
          sudo aptitude purge '~n ^php' -f -y 2>&1 || true
          sudo aptitude purge '~n ^dotnet' -f -y 2>&1 || true
          sudo apt-get autoremove -y 2>&1 || true
          sudo apt-get autoclean -y 2>&1 || true
          echo "Some packages purged"

      - name: Check disk space post-cleanup
        run: |
          sudo dpkg-query -Wf '${Installed-Size}\t${Package}\n' | sort -nr | head
          df . -h
          sudo du /usr/ -hx -d 4 --threshold=1G | sort -hr | head

      # Install the cosign tool except on PR
      # https://github.com/sigstore/cosign-installer
      - name: Install cosign
        if: github.event_name != 'pull_request'
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 #v4.1.2
        with:
          cosign-release: 'v2.2.4'

      # Set up BuildKit Docker container builder to be able to build
      # multi-platform images and export cache
      # https://github.com/docker/setup-buildx-action
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0

      # Login against a Docker registry except on PR
      # https://github.com/docker/login-action
      - name: Log into registry ${{ env.REGISTRY }}
        if: github.event_name != 'pull_request'
        uses: docker/login-action@06fb636fac595d6fb4b28a5dfcb21a6f5091859c # v4.5.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ env.USERNAME }}
          password: ${{ env.PASSWORD }}

      # Extract metadata (tags, labels) for Docker
      # https://github.com/docker/metadata-action
      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6.2.0
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            # Only use arch-specific tags for the individual build push
            type=raw,value=${{ matrix.platform }}
            type=raw,value=${{ github.sha }}-${{ matrix.platform }}

      # Build and push Docker image with Buildx (don't push on PR)
      # https://github.com/docker/build-push-action
      - name: Build and push Docker image
        id: build-and-push
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          platforms: ${{ matrix.docker_platform }}
          context: .
          file: Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ matrix.platform }}
          cache-to:   type=gha,scope=${{ matrix.platform }},mode=max

      # Sign the resulting Docker image digest except on PRs.
      # This will only write to the public Rekor transparency log when the Docker
      # repository is public to avoid leaking data.  If you would like to publish
      # transparency data even for private images, pass --force to cosign below.
      # https://github.com/sigstore/cosign
      - name: Sign the published Docker image
        if: ${{ github.event_name != 'pull_request' }}
        env:
          # https://docs.github.com/en/actions/security-guides/security-hardening-for-github-actions#using-an-intermediate-environment-variable
          TAGS: ${{ steps.meta.outputs.tags }}
          DIGEST: ${{ steps.build-and-push.outputs.digest }}
        # This step uses the identity token to provision an ephemeral certificate
        # against the sigstore community Fulcio instance.
        run: echo "${TAGS}" | xargs -I {} cosign sign --yes {}@${DIGEST}

  merge:
    name: Merge multi-arch images
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request'
    permissions:
      contents: read
      packages: write
      id-token: write

    steps:
      - name: Install cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 #v4.1.2
        with:
          cosign-release: 'v2.2.4'

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0

      - name: Log into registry ${{ env.REGISTRY }}
        uses: docker/login-action@06fb636fac595d6fb4b28a5dfcb21a6f5091859c # v4.5.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ env.USERNAME }}
          password: ${{ env.PASSWORD }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6.2.0
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            # always tag the multi-arch manifest as :docker
            type=raw,value=docker

      - name: Create and push multi-arch manifests
        run: |
          set -euo pipefail
          IMAGE="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}"
      
          # steps.meta.outputs.tags is a newline-separated list of fully-qualified tags
          while read -r TAG; do
            [ -z "$TAG" ] && continue
      
            echo "Creating multi-arch image for ${TAG}"
            docker buildx imagetools create \
              -t "$TAG" \
              "${IMAGE}:amd64" \
              "${IMAGE}:arm64"
          done <<< "${{ steps.meta.outputs.tags }}"

      - name: Sign the multi-arch image manifests
        env:
          TAGS: ${{ steps.meta.outputs.tags }}
        run: |
          # cosign can sign by tag; Rekor entry will use the digest under the hood
          echo "${TAGS}" | xargs -I {} cosign sign --yes {}
